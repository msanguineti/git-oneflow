module.exports = {
  // Main (production) branch name
  main: 'master',
  // Use development branch?
  usedev: false,
  // Feature branch name
  feature: 'feature',
  // Release branch name
  release: 'release',
  // Hotfix branch name
  hotfix: 'hotfix',
  // Integration method to use (see https://www.endoflineblog.com/oneflow-a-git-branching-model-and-workflow#feature-branches)
  integration: 1,
  // Use interactve rebase (git rebase -i)?
  interactive: 'always'
}
